Provenance for ExtrudeM_2007.dwg

Author app:              Autocad 2027.1 (campaign authoring app; the file
                         was authored to the S18.7 ExtrudeM recipe row —
                         correct this line if the maintainer's notes differ)
Authoring date:           2026-09-24
Author session (the S18.7 differential recipe, row ExtrudeM):
  new drawing on a blank/default template, zero pre-existing entities
  set solid-history recording ON
  EXTRUDE a CIRCLE center 0,0,0 radius 1 with MOde = Surface, height 2
  (the typed command sequence above is the recipe row; correct it from
   the authoring session's notes if it differs)

Save format:              DWG 2007 (AC1021)

Qualification (verified by acadrust harness, 2026-09-24, at landing):
  gold dwgread: zero Error lines
  wire version: AC1021
  object census: 216 objects, 3 UNKNOWN_OBJ (sizes [24, 31, 2731])
  EXTRUDEDSURFACE: registered
  ACSH_HISTORY_CLASS: registered
  gold JSON parse: ok (with the standard -nan shim where applicable)
  differential invariants (S18.7):
    quartet bit-identity: the four ExtrudeM versions carry bit-identical tails
    corpus: ExtrudeM_2007.dwg QUARANTINED (not in the corpus run — see the note below)
    QUARANTINED: the file is a valid specimen (gold zero errors; EXTRUDEDSURFACE class registered; the ASSOCEXTRUDEDSURFACEACTIONBODY typed with the named ExtrusionHeight/ExtrusionTaperAngle pab-values) but silver does not yet parse the R2007+ surface entities / surface action bodies — the corpus zero-keeping rule excludes it until the surface-parser row lands

Covered campaign rows (see IMPLEMENTATION.md S18.7 + F2.3): ExtrudeM

QUARANTINE NOTE (2026-09-24): this specimen lives outside the corpus
tree (tests_quarantine/) because silver does not yet parse the
R2007+ surface entities and the ASSOC surface action bodies this
file exercises — the corpus zero-keeping rule excludes it until the
surface-parser row lands (see S18.7's M-row notes). It remains the
typed semantic anchor the recipe wanted: gold parses the surface
class + the action body with the named parameters.
