Provenance for ExtrudeH_2010.dwg

Author app:              Autocad 2027.1 (campaign authoring app; the file
                         was authored to the S18.7 ExtrudeH recipe row —
                         correct this line if the maintainer's notes differ)
Authoring date:           2026-09-24
Author session (the S18.7 differential recipe, row ExtrudeH):
  new drawing on a blank/default template, zero pre-existing entities
  set solid-history recording ON
  EXTRUDE a CIRCLE center 0,0,0 radius 1, typed height 5
  (the typed command sequence above is the recipe row; correct it from
   the authoring session's notes if it differs)

Save format:              DWG 2010 (AC1024)

Qualification (verified by acadrust harness, 2026-09-24, at landing):
  gold dwgread: zero Error lines
  wire version: AC1024
  object census: 204 objects, 3 UNKNOWN_OBJ (sizes [17, 17, 69])
  ACSH_EXTRUSION_CLASS: registered
  ACSH_HISTORY_CLASS: registered
  gold JSON parse: ok (with the standard -nan shim where applicable)
  differential invariants (S18.7):
    quartet bit-identity: the four ExtrudeH versions carry bit-identical tails
    corpus: ExtrudeH_2010.dwg enters at read 0 / write 0
    OBSERVED: the tail stays 208 bits and differs from the landed Extrude at exactly 2 bits inside the direction z raw (2.0 -> 5.0) — the height lives in the direction 3BD head and NOWHERE else; the 64-bit payload does not encode height

Covered campaign rows (see IMPLEMENTATION.md S18.7 + F2.3): ExtrudeH
