Provenance for PolysolidL_2013.dwg

Author app:              Autocad 2027.1 (campaign authoring app; the file
                         was authored to the S18.7 PolysolidL recipe row —
                         correct this line if the maintainer's notes differ)
Authoring date:           2026-09-24
Author session (the S18.7 differential recipe, row PolysolidL):
  new drawing on a blank/default template, zero pre-existing entities
  set solid-history recording ON
  POLYSOLID -> Height 5 -> width 3; path 0,0,0 -> 5,0,0, ENTER to end
  (the typed command sequence above is the recipe row; correct it from
   the authoring session's notes if it differs)

Save format:              DWG 2013 (AC1027)

Qualification (verified by acadrust harness, 2026-09-24, at landing):
  gold dwgread: zero Error lines
  wire version: AC1027
  object census: 154 objects, 3 UNKNOWN_OBJ (sizes [14, 21, 221])
  record classes: ACSH_SWEEP_CLASS + ACSH_HISTORY_CLASS + 3DSOLID (the solid-history shape)
  gold JSON parse: ok (with the standard -nan shim where applicable)
  differential invariants (S18.7):
    quartet bit-identity: the four PolysolidL versions carry bit-identical tails
    corpus: PolysolidL_2013.dwg enters at read 0 / write 0
    OBSERVED (quad bit-identical at 1418 bits — the same length as X
      despite the half path; corpus 0/0): THE 2.0109 PROBE IS DECISIVE —
      raw@998 = 2.0108894202880947 in BOTH L (path 5) and X (path 12),
      BIT-FOR-BIT IDENTICAL: the single is PROFILE-DERIVED, not
      path-derived. @900 (-3.90625) and @1092 (32.0625) likewise track
      the profile (same values as X; W profile differs); 4.00024414192312
      the record constant (third confirmation); the width single @1084 =
      3.0 (the shared width). The segment end reads (5.0, 0.0) — the only
      path-derived entry. The post-corner singles stream is closed:
      every entry is profile-derived or the segment end; the stream walk
      (the decoder row) collects them.
Covered campaign rows (see IMPLEMENTATION.md S18.7 + F2.3): PolysolidL
