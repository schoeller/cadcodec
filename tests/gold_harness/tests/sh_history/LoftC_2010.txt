Provenance for LoftC_2010.dwg

Author app:              Autocad 2027.1 (campaign authoring app; the file
                         was authored to the S18.7 LoftC recipe row —
                         correct this line if the maintainer's notes differ)
Authoring date:           2026-09-24
Author session (the S18.7 differential recipe, row LoftC):
  new drawing on a blank/default template, zero pre-existing entities
  set solid-history recording ON
  LOFT CIRCLE 3,4,0 r 1.5 + CIRCLE 3,4,7 r 1.5 — re-authored with MOde = Solid explicitly set at the prompt (the first attempt landed as a surface and was quarantined)
  (the typed command sequence above is the recipe row; correct it from
   the authoring session's notes if it differs)

Save format:              DWG 2010 (AC1024)

Qualification (verified by acadrust harness, 2026-09-24, at landing):
  gold dwgread: zero Error lines
  wire version: AC1024
  object census: 209 objects, 3 UNKNOWN_OBJ (sizes [17, 24, 126])
  record classes: ACSH_LOFT_CLASS + ACSH_HISTORY_CLASS + 3DSOLID (the solid-history shape)
  gold JSON parse: ok (with the standard -nan shim where applicable)
  differential invariants (S18.7):
    quartet bit-identity: the four LoftC versions carry bit-identical tails
    corpus: LoftC_2010.dwg enters at read 0 / write 0
    OBSERVED (684 bits, the prediction confirmed):
    THE LOFT RAW RUN IS FULLY NAMED — the WORLD reading confirmed:
      raws [3.0, 4.0, 1.5, 3.0, 4.0, 7.0, 1.5, pi/2, pi/2] parse as
      PER-SECTION [center.x][center.y][height][radius] runs + the
      draft pair: section 1 = (3, 4, z 0.0 elided as a short, r 1.5),
      section 2 = (3, 4, z 7.0, r 1.5), then [pi/2, pi/2]. The landed
      original retro-fits the same reading exactly: (0,0) r 1 elided,
      (2.006 -> 2.0, 2.006 -> 2.0, z 5.0, r 0.2996 -> 0.3), then the
      draft pair — the world reading; the offsets PRESENT in the raws.
Covered campaign rows (see IMPLEMENTATION.md S18.7 + F2.3): LoftC
