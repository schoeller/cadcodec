Provenance for PolysolidW_2007.dwg

Author app:              Autocad 2027.1 (campaign authoring app; the file
                         was authored to the S18.7 PolysolidW recipe row —
                         correct this line if the maintainer's notes differ)
Authoring date:           2026-09-24
Author session (the S18.7 differential recipe, row PolysolidW):
  new drawing on a blank/default template, zero pre-existing entities
  set solid-history recording ON
  POLYSOLID -> Height 3 -> width 7; path 0,0,0 -> 12,0,0, ENTER to end
  (the typed command sequence above is the recipe row; correct it from
   the authoring session's notes if it differs)

Save format:              DWG 2007 (AC1021)

Qualification (verified by acadrust harness, 2026-09-24, at landing):
  gold dwgread: zero Error lines
  wire version: AC1021
  object census: 206 objects, 3 UNKNOWN_OBJ (sizes [22, 22, 226])
  record classes: ACSH_SWEEP_CLASS + ACSH_HISTORY_CLASS + 3DSOLID (the solid-history shape)
  gold JSON parse: ok (with the standard -nan shim where applicable)
  differential invariants (S18.7):
    quartet bit-identity: the four PolysolidW versions carry bit-identical records
    corpus: PolysolidW_2007.dwg enters at read 0 / write 0
    OBSERVED (quad bit-identical, 1418 bits — the same length as PolysolidX
      with the swapped profile): the decode reads corners (plus/minus 3.5,
      {0, 3}) exactly as predicted (width 7 -> plus/minus 3.5, height 3);
      the segment end (12.0, 0) lands clean where X carried 12.00001 drag
      residue; the post-corner single at bit 1084 moved 3.0 -> 7.0 with
      the width — THE WIDTH SINGLE IS NAMED; 4.00024414192312 is
      INVARIANT (a record constant, present in the original/ X/ W);
      2.0109 is path-family-shared (both X-direction typed paths) with
      low-bit precision noise; X's minus-19594/ 8614-class entries are
      drag residue (zeroed/absent in this cleaner authoring)
Covered campaign rows (see IMPLEMENTATION.md S18.7 + F2.3): PolysolidW
