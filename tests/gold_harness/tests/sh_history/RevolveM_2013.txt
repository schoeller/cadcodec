Provenance for RevolveM_2013.dwg

Author app:              Autocad 2027.1 (campaign authoring app; the file
                         was authored to the S18.7 RevolveM recipe row —
                         correct this line if the maintainer's notes differ)
Authoring date:           2026-09-24
Author session (the S18.7 differential recipe, row RevolveM):
  new drawing on a blank/default template, zero pre-existing entities
  set solid-history recording ON
  REVOLVE a CIRCLE center 2,0,0 radius 0.8 about axis 0,0,0 -> 0,1,0 with MOde = Surface, angle 180
  (the typed command sequence above is the recipe row; correct it from
   the authoring session's notes if it differs)

Save format:              DWG 2013 (AC1027)

Qualification (verified by acadrust harness, 2026-09-24, at landing):
  gold dwgread: zero Error lines
  wire version: AC1027
  object census: 165 objects, 3 UNKNOWN_OBJ (sizes [19, 43, 870])
  REVOLVEDSURFACE: registered
  ACSH_HISTORY_CLASS: registered
  gold JSON parse: ok (with the standard -nan shim where applicable)
  differential invariants (S18.7):
    quartet bit-identity: the four RevolveM versions carry bit-identical tails
    corpus: RevolveM_2013.dwg lands in-corpus (the surface-parser row closed 2026-09-24: read 0 / write 0)
    landed: the REVOLVEDSURFACE-class record parses through the gold-shadow object walk (gold's classes read desyncs on the 2027.1 class tables, so the entity-class records decode as gold's UNKNOWN_OBJ); the ASSOCREVOLVEDSURFACEACTIONBODY fields project for both grammar branches — read 0 / write 0

Covered campaign rows (see IMPLEMENTATION.md S18.7 + F2.3): RevolveM

LANDED NOTE (2026-09-24, the surface-parser row): the specimen moved into
the corpus tree (tests/gold_harness/tests/sh_history/). Silver now walks
the R2007+ surface-entity records exactly as gold does — the gold-shadow
classes walk reproduces gold's BS/BS tail read (its cursor derails on the
AutoCAD-2027.1 class tables from the 10th entry on, so gold decodes the
EXTRUDEDSURFACE/LOFTEDSURFACE/REVOLVEDSURFACE records through the
unknown-OBJECT walk) — and projects the ASSOC surface action bodies for
both shapes (the PRE-R_2013b pab fields + the R2013+ sab-only grammar).
Read 0 / write 0 on all four versions.
