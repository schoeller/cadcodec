Provenance for ExtrudeCSurf_2018.dwg

Author app:              Autocad 2027.1 (campaign authoring app; the file
                         was authored to the S18.7 ExtrudeC recipe row —
                         correct this line if the maintainer's notes differ)
Authoring date:           2026-09-24
Author session (the S18.7 differential recipe, row ExtrudeC, the surface-mode first attempt):
  new drawing on a blank/default template, zero pre-existing entities
  set solid-history recording ON
  EXTRUDE a CIRCLE center 2,3,0 radius 1, height 2 (the intended MOde = Solid authoring; the session left MOde = Surface)
  (the typed command sequence above is the recipe row; correct it from
   the authoring session's notes if it differs)

Save format:              DWG 2018 (AC1032)

Qualification (verified by acadrust harness, 2026-09-24, at landing):
  gold dwgread: zero Error lines
  wire version: AC1032
  object census: 151 objects, 3 UNKNOWN_OBJ (sizes [19, 43, 964])
  record classes: EXTRUDEDSURFACE/LOFTEDSURFACE + the ASSOC surface action body (NO ACSH classes, NO 3DSOLID — the surface-mode shape)
  gold JSON parse: ok (with the standard -nan shim where applicable)
  differential invariants (S18.7):
    quartet bit-identity: the four ExtrudeC versions carry bit-identical records
    corpus: ExtrudeCSurf_2018.dwg lands in-corpus (the surface-parser row closed 2026-09-24: read 0 / write 0)

Covered campaign rows (see IMPLEMENTATION.md S18.7 + F2.3): ExtrudeCSurf (the ExtrudeC surface-mode first attempt; the Solid re-author is the in-corpus ExtrudeC fixture)

LANDED NOTE (2026-09-24, the surface-parser row): the surface-mode shape
      parses read 0 / write 0 — the surface-entity record walks the
      gold-shadow object path (gold's unknown-OBJECT decode of the
      entity-class records) and the typed ASSOC surface action body
      projects its sab grammar. The EXTRUDEDSURFACE stays the surface-mode OFFSET
      specimen; the Solid re-authors of the same recipe row are already
      in-corpus.
